
  package com.example.demo; import java.util.Arrays;
import java.util.Date;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import
  org.springframework.data.mongodb.core.aggregation.ConvertOperators.ToString;
  
  import com.example.demo.models.details;
import com.example.demo.repository.railwayRepository;
  
  
  public class JavaStreamExample {
  
  public static void main(String[] args) {
  
	  
	 
		List<details>trainDetails;
	   Date d=new Date();
	   
		
		/*
								 * Person result1 = persons.stream() .filter((p) -> "jack".equals(p.getName())
								 * && 20 == p.getAge()) .findAny() .orElse(null);
								 * 
								 * System.out.println("result 1 :" + result1);
								 * 
								 * //or like this Person result2 = persons.stream() .filter(p -> { if
								 * ("jack".equals(p.getName()) && 20 == p.getAge()) { return true; } return
								 * false; }).findAny() .orElse(null);
								 * 
								 * System.out.println("result 2 :" + result2);
		*/
  
  }
  
  
  }
 